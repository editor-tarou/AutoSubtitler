# AutoSubtitle — Python dependencies
# Install with:  pip install -r requirements.txt

# Core transcription
openai-whisper
stable-ts

# PyTorch — install the CUDA version for GPU acceleration:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Or CPU-only (smaller download):
torch
